import { UnterstrichEntfernenPipe } from './unterstrich-entfernen.pipe';

describe('UnterstrichEntfernenPipe', () => {
  it('create an instance', () => {
    const pipe = new UnterstrichEntfernenPipe();
    expect(pipe).toBeTruthy();
  });
});
