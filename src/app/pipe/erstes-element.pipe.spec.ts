import { ErstesElementPipe } from './erstes-element.pipe';

describe('ErstesElementPipe', () => {
  it('create an instance', () => {
    const pipe = new ErstesElementPipe();
    expect(pipe).toBeTruthy();
  });
});
